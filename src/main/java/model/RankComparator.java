package model;

import java.util.Comparator;

public class RankComparator {
//
//    //(a,b)->a.getName().compareToIgnoreCase(b.getName())
//
//    public Comparator<Doctor> startTimeComparator = (a,b)-> a.getTimeSlotList()
}
